class Compteur {
    constructor(medias, photographer, mock) {
        this.medias = medias
        this.photographer = photographer
        this.mock = mock
    }


    likes() {
        const med = new Gallery()
        console.log("mediacompteur==>" + this.med)
        console.log("photographecompteur==>" + this.photographer)
        console.log("mockcompteur==>" + this.mock)




    };
}